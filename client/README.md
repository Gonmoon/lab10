# Belpost Subscriptions Client (React + Redux)

## Установка и запуск

```bash
cd client
npm install
npm run dev
```

По умолчанию клиент ожидает, что backend доступен по адресу `http://localhost:5000/api`.
